# .env — copy this file, fill in your values, never commit to git
# Used by both docker-compose.yml and the Python ETL directly

# ── Source PostgreSQL (READ-ONLY — your existing ClearSight DB) ──
SOURCE_HOST=your-source-postgres-host
SOURCE_PORT=5432
SOURCE_DB=clearsight_db
SOURCE_USER=readonly_user
SOURCE_PASS=readonly_password

# ── Target PostgreSQL (WRITABLE — spun up by docker-compose) ────
# If using docker-compose, TARGET_HOST=target_db (internal Docker name)
# If using external RDS, set TARGET_HOST to your RDS endpoint
TARGET_HOST=target_db
TARGET_PORT=5432
TARGET_DB=clearsight_reporting
TARGET_USER=etl_user
TARGET_PASS=choose_a_strong_password_here
